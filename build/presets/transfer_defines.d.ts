import { MYSQL_CREDENTIALS } from "mysql-tools";
export declare const transferprepare: ({ DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, DATABASES }: MYSQL_CREDENTIALS) => Promise<boolean>;
//# sourceMappingURL=transfer_defines.d.ts.map