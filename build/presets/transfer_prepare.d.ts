import { MYSQL_CREDENTIALS } from "../defines";
export declare const transfer_prepare: ({ DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, DATABASES }: MYSQL_CREDENTIALS) => Promise<boolean>;
//# sourceMappingURL=transfer_prepare.d.ts.map