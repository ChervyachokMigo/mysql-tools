import { MYSQL_CREDENTIALS } from "../defines";
export declare const export_prepare: ({ DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, DATABASES }: MYSQL_CREDENTIALS) => Promise<boolean>;
//# sourceMappingURL=export_defines.d.ts.map